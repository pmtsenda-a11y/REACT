/* ============================================
   EJERCICIO 04: Componentes con CVA
   Archivo: utils.ts (starter)
   ============================================ */

// ============================================
// PASO 2: Helper para combinar clases Tailwind
// cn() es un patrón común en proyectos con Tailwind
// Combina clsx (para condicionales) + tailwind-merge (para conflictos)
// ============================================

// import { clsx, type ClassValue } from 'clsx';
// import { twMerge } from 'tailwind-merge';

// Descomenta la función:
// /**
//  * Combina clases de Tailwind de forma inteligente
//  * - Soporta clases condicionales como clsx
//  * - Resuelve conflictos de utilidades (ej: px-4 + px-6 = px-6)
//  */
// export const cn = (...inputs: ClassValue[]) => {
//   return twMerge(clsx(inputs));
// };

export {};
